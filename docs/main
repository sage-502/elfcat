# main.c 설명

## 개요
`main.c`는 ELF 취약점 분석 프로그램의 실행 진입점이다.  
현재는 parser가 없는 상태이므로 테스트용 ELF를 생성하여 분석하는 구조로 작성되어 있다.

## 주요 흐름

1. 테스트용 ELF 생성
2. 취약점 분석 수행
3. 결과 출력
4. 동적 메모리 해제

## 코드 흐름

```c
elf = create_test_elf();
v = analyze_vulnerability(elf);
print_vuln(v);
free_vuln(&v);
free_elf(elf);
